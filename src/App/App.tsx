import React from 'react';
import * as styles from './App.module.scss';

export const App = (): JSX.Element => {
  return (
		<React.Fragment>
			<h1 className={styles.style}>Hello</h1>
		</React.Fragment>
  );
};
